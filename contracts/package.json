{
  "name": "titan-contracts",
  "version": "1.0.0",
  "description": "TITAN (TTN) Module 1 — BEP-20 token + protocol contract skeletons",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bscTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@openzeppelin/merkle-tree": "^1.0.8",
    "chai": "^4",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.0.2",
    "dotenv": "^16.4.5"
  }
}
